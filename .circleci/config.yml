version: 2

jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run:
          command: |
            echo "Some Line" > /tmp/newfile.txt
      - store_artifacts:
          path: /tmp/newfile.txt

  test:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run:
          command: |
            cd tmp
            cat newfile.txt

workflows:
  version: 2
  commit-workflow:
    jobs:
      - build
      - test:
          requires:
            - build
