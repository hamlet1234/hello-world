version: 2

jobs:
  build1:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run:
          command: |
            cd checker
            npm install
            ts-node checkAll.ts

workflows:
  version: 2
  commit-workflow:
    jobs:
      - build1
